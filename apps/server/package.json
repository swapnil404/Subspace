{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@Subspace/auth": "workspace:*",
    "@Subspace/db": "workspace:*",
    "@Subspace/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.11.9",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@Subspace/config": "workspace:*",
    "@types/bun": "catalog:",
    "bun-types": "^1.3.9",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
